# ============================================
# ШАГ 1: Выбираем базовый образ
# ============================================
# Используем официальный образ Python 3.11
# slim версия занимает меньше места
FROM python:3.11-slim

# ============================================
# ШАГ 2: Устанавливаем системные зависимости
# ============================================
# Обновляем пакеты и устанавливаем curl для healthcheck
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# ============================================
# ШАГ 3: Настраиваем рабочую директорию
# ============================================
# Создаем и переходим в рабочую папку
WORKDIR /app

# ============================================
# ШАГ 4: Копируем зависимости
# ============================================
# Сначала копируем только requirements.txt
# Это позволяет Docker кэшировать установку зависимостей
COPY requirements.txt .

# ============================================
# ШАГ 5: Устанавливаем зависимости Python
# ============================================
RUN pip install --no-cache-dir -r requirements.txt

# ============================================
# ШАГ 6: Копируем исходный код
# ============================================
# Копируем всю папку src
COPY src/ ./src/

# ============================================
# ШАГ 7: Настраиваем переменные окружения
# ============================================
# Устанавливаем значения по умолчанию
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1
ENV API_HOST=0.0.0.0
ENV API_PORT=8000

# ============================================
# ШАГ 8: Открываем порт
# ============================================
# Говорим Docker, что приложение использует порт 8000
EXPOSE 8000

# ============================================
# ШАГ 9: Запускаем приложение
# ============================================
# Команда для запуска приложения
CMD ["uvicorn", "src.api:app", "--host", "0.0.0.0", "--port", "8000"]

# ============================================
# МЕТКИ (опционально, но полезно)
# ============================================
LABEL maintainer="Ваше Имя <ваш@email.com>"
LABEL description="Password Complexity Checker"
LABEL version="1.0.0"